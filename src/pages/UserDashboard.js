import React from 'react';
import UserView from '../views/UserDashboard';

const UserDashboard = () => {
    return (
        <div>
          <UserView />
        </div>
    );
};

export default UserDashboard;